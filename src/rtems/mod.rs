////////////////////////////////////////////////////////////////////////////////
// rtems api wrappers
////////////////////////////////////////////////////////////////////////////////

use core::ffi::*;

////////////////////////////////////////////////////////////////////////////////
// kernel io support

extern "C" {
    pub fn printk(fmt: *const c_char, ...) -> c_int;
}

////////////////////////////////////////////////////////////////////////////////
// kernel shutdown

extern "C" {
    pub fn rtems_panic(fmt: *const c_char, ...) -> !;
    pub fn rtems_shutdown_executive(fatal_code: u32);
}
